##  SwiftExtensions

Everytime I start a new project, I always have to repeat some commonly used extensions to standard Swift classes and types. Think of examples such as `init` for `UIStackView` that can accept all properties, or + symbol for dictionaries. This package is a compilation of extensions that I commonly use. 

